const initialState = {
  // rien pour le moment
};

const cityReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cityReducer;
